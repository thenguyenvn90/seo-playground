# Build artefacts — rebuilt inside the image
node_modules
.next
out
build

# VCS + IDE
.git
.gitignore
.vscode
.idea

# Env + secrets — inject via Coolify
.env
.env.*
!.env.example

# Legacy SQLite cache from pre-Postgres dev sessions
*.db
*.db-shm
*.db-wal

# Prisma generated client — regenerated by `prisma generate` during build
src/generated

# OS noise
.DS_Store
Thumbs.db

# Repo docs/screenshots not needed at runtime
/public/screenshot-*.png
/public/review-analysis-screenshot.png
